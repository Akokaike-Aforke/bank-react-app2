// const startLogOutTimer = (refElement) => {
//   let time = 100;
//   setInterval(() => {
//     refElement.current.textContent = time;
//     time--;
//   }, 1000);
// };

const startLogOutTimer = (elem) => {
  // let time = 40;
  // const tick = () => {
  //     const min = String(Math.trunc(time/60)).padStart(2,0);
  //     const sec = String(time%60).padStart(2,0);
  //   elem.current.textContent = `${min}:${sec}`;
  // if (time === 0) {
  //   clearInterval(timerSet);
  // }
  //   time--;
  // }
  // tick()
  // const timerSet =setInterval(tick, 1000);
};
export default startLogOutTimer;
